[package]
name = "clustering-wasm"
version = "0.1.0"
edition = "2021"
rust-version = "1.78"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
music-clustering = { path = "../clustering-rs", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
